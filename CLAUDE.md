# 项目规则 / Project Rules

## 对话语言
- 始终使用**简体中文**与用户对话。

## 命令展示格式
- 每当需要让用户运行命令时，必须将命令放在独立的代码块（```bash ... ```）中展示，确保用户可以一键复制。
- 不得将命令内嵌在句子里（如"请运行 `xxx` 查看效果"这种行内格式不可用于需要用户执行的命令）。
- 若有多条命令，每条单独一个代码块，或在同一代码块内用换行分隔，保持可直接粘贴执行。
